{
  "schema_version": 1,
  "min_app": "1.0.0",
  "note": "Overlay on the bundled catalog. Launch + foreground fetch. relocate moves existing rows/series between recommended and legacy without restating Hub fields. New models[] rows are prepended (latest first). Unknown model_types are ignored. Empty models + empty relocate leaves the bundled list unchanged. announcements[].image_url must be https.",
  "hide": [],
  "relocate": [],
  "models": [],
  "announcements": []
}
